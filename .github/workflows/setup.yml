name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run install script
      run: |
        echo Trying to run `./index.sh`
        ./index.sh
